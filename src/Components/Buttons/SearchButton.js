import * as React from 'react';
import Button from '@mui/material/Button';

export default function SearchButton() {
    return (
        <Button variant="contained" style={{boxShadow:"30px,30px,5px"}} >Search</Button>
    );
}